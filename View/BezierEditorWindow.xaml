<Window x:Class="BezierEasing.View.BezierEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BezierEasing.View.BezierEditor" d:DataContext="{d:DesignInstance Type=local:BezierEditorCanvasViewModel}"
        mc:Ignorable="d"
        Title="ベジェエディタ" Height="450" Width="800">
    <Window.InputBindings>
        <KeyBinding Modifiers="Ctrl" Key="Z" Command="{Binding UndoCommand}"/>
        <KeyBinding Modifiers="Ctrl" Key="Y" Command="{Binding RedoCommand}"/>
    </Window.InputBindings>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.5*"/>
            <ColumnDefinition Width="0.5*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ComboBox Width="150" HorizontalAlignment="Left" ItemsSource="{Binding EnumValues}" SelectedItem="{Binding SelectedEnumValue}" Grid.Column="0"/>
                <Button Content="Del" Width="75" HorizontalAlignment="Left" Command="{Binding DeleteActivePointCommand}"  Grid.Column="1"/>
            </Grid>

            <Viewbox Grid.Row="1" Stretch="Uniform">
                <local:BezierEditorCanvas />
            </Viewbox>
            

        </Grid>
        
    </Grid>
</Window>
