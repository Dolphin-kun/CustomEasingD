<UserControl x:Class="BezierEasing.View.BezierEditor.BezierEditorCanvas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:local="clr-namespace:BezierEasing.View.BezierEditor"
			 xmlns:theme="clr-namespace:YukkuriMovieMaker.Theme;assembly=YukkuriMovieMaker.Plugin"
			 mc:Ignorable="d" d:Width="300" Name="this">
    <Viewbox Stretch="Uniform">
        <Border Background="{DynamicResource {x:Static theme:YMM4Colors.IconBorderBrushKey}}" Margin="20">
            <ContentControl MouseDoubleClick="ContentControl_MouseDoubleClick">
                <Canvas x:Name="BezierEditor" Width="300" Height="300" Background="Transparent"
                        MouseLeftButtonDown="BezierEditor_MouseLeftButtonDown"
                        MouseLeftButtonUp="BezierEditor_MouseLeftButtonUp"
                        MouseMove="BezierEditor_MouseMove">
                    <Canvas.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform x:Name="CanvasScaleTransform" ScaleX="1.0" ScaleY="1.0"/>
                            <TranslateTransform x:Name="CanvasTranslateTransform"/>
                        </TransformGroup>
                    </Canvas.RenderTransform>

                    
                    <Path x:Name="HandlePath" Stroke="LightGray" StrokeThickness="1" />
                    
                    <Path x:Name="BezierPath" Stroke="Black" StrokeThickness="2" Fill="Transparent"/>

                </Canvas>
            </ContentControl>
        </Border>
    </Viewbox>
</UserControl>